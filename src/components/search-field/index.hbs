<div class="searchWrapper">
    <img src="/assets/search.png" alt="search" width="20" height="20" />
    <input
        type="{{type}}"
        id="{{id}}"
        name="{{name}}"
        placeholder="{{placeholder}}"
        pattern="{{pattern}}"
        required
        class="search"
    />
</div>