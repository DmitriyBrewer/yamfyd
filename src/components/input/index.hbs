<input
    type={{type}}
    id={{id}}
    name={{name}}
    placeholder={{placeholder}}
    {{#if isRequired}}
        required
    {{/if}}
    class="input {{#if className}} {{className}} {{/if}}"
    pattern={{pattern}}
/>